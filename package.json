{
  "scripts": {
    "format": "npx prettier --write .",
    "build": "npx tailwindcss -i ./src/input.css -o ./assets/css/main.css && npm run format"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "prettier": "^3.2.5",
    "prettier-plugin-go-template": "^0.0.15",
    "tailwindcss": "^3.4.1"
  }
}
